# MULTILSB atomic admission gate - v0.30.0

This release freezes the exact 120-instance admission set, the nine-file model extension surface, baseline hashes, rollback rules and the target registry size of 8,025. It does not claim that a source-declared MIPLIB overlap is an exact identity.

Admission is all-or-nothing. Existing canonical XML files cannot be rewritten. The 120 candidates remain blocked until shared production-family setup serialization, validation, classification and solver semantics compile and pass their tests.
