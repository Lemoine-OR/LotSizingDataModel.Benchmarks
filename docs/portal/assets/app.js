document.querySelectorAll('[data-table-filter]').forEach(input=>{const table=input.closest('.section').querySelector('tbody');const counter=input.closest('.section').querySelector('[data-visible-count]');input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();let visible=0;table.querySelectorAll('tr').forEach(row=>{const show=!q||row.dataset.search.includes(q);row.hidden=!show;if(show)visible++});if(counter)counter.textContent=visible.toLocaleString()})});
