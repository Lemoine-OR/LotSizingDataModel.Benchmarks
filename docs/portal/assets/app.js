document.querySelectorAll('.section').forEach(section=>{const input=section.querySelector('[data-table-filter]');const table=section.querySelector('tbody');if(!input||!table)return;const counter=section.querySelector('[data-visible-count]');let mode='all';const apply=()=>{const q=input.value.trim().toLowerCase();let visible=0;table.querySelectorAll('tr').forEach(row=>{const textOk=!q||row.dataset.search.includes(q);const modeOk=mode==='all'||(mode==='known'&&row.dataset.result==='true')||(mode==='proven'&&row.dataset.proven==='true');const show=textOk&&modeOk;row.hidden=!show;if(show)visible++});if(counter)counter.textContent=visible.toLocaleString()};input.addEventListener('input',apply);section.querySelectorAll('[data-result-filter]').forEach(button=>button.addEventListener('click',()=>{mode=button.dataset.resultFilter;section.querySelectorAll('[data-result-filter]').forEach(b=>b.classList.toggle('active',b===button));apply()}))});
