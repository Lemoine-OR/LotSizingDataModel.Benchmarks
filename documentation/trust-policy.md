# Result trust policy

Published objective values and lower bounds are evidence, not automatically verified results.

A value becomes VERIFIED only when the exact instance identity is established and, for a complete solution,
LotSizingDataModel.Checker independently validates feasibility and objective evaluation.

Derived or aggregated instances never inherit a parent's result without proof of exact equivalence.
